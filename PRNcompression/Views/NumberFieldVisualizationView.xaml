<UserControl x:Class="PRNcompression.Views.NumberFieldVisualizationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:vm="clr-namespace:PRNcompression.ViewModels"
             xmlns:local="clr-namespace:PRNcompression.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.DataContext>
        <vm:NumberFieldVisualizationViewModel/>
    </UserControl.DataContext>
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="13*"/>
        </Grid.ColumnDefinitions>
        <StackPanel Grid.Column="0">
            <TextBlock Text="Отобразить чисел:"/>
            <ComboBox ItemsSource="{Binding NumQuantities}" SelectedItem="{Binding SelectedQuantity}"/>
            <TextBlock Text="Стартовое число:"/>
            <TextBox Text="{Binding NumStr, Mode=TwoWay}"/>
            <Button Content="Отобразить" Command="{Binding VisualizeFromNumberCommand}"/>
        </StackPanel>
        <Grid Grid.Column="1">
            <ContentControl Content="{Binding MyDataGrid}"/>
        </Grid>
       
    </Grid>
</UserControl>
